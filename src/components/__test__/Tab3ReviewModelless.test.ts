import Tab3ReviewModelless from "../Tab3ReviewModelless.vue";
test("FETCH: propSelectedAdd/Remove", () => {});

describe("stateFullActive", function () {
  test("stateBasicActive로 상태 전환", () => {});
});

describe("stateBasicAcitve", function () {
  test("stateFullActive로 상태 전환", () => {});
});

describe("삭제하기 버튼 선택시 함수동작", function () {
  test("EMIT: 삭제 페이지로 이동");
  test("stateBasicActive로 상태 전환", () => {});
});
